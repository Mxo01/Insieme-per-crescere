name: Deploy Dashboard to GitHub Pages

on:
    pull_request:
        types: [closed]
        paths:
            - 'dashboard/**'

permissions:
    contents: write

jobs:
    deploy_dashboard:
        if: ${{ github.event.pull_request.head.repo.full_name == github.repository && github.event.pull_request.merged == true && github.ref == 'refs/heads/main' }}
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: 22
                  cache: 'npm'
                  cache-dependency-path: dashboard/package-lock.json

            - name: Install dependencies
              working-directory: dashboard
              run: npm ci

            - name: Build
              working-directory: dashboard
              run: npm run build -- --base-href /insieme-per-crescere/dashboard/

            - name: Deploy to GitHub Pages
              uses: peaceiris/actions-gh-pages@v4
              with:
                  github_token: ${{ secrets.GITHUB_TOKEN }}
                  publish_dir: dashboard/dist/dashboard/browser
                  destination_dir: dashboard
                  keep_files: true
